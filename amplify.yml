version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build # Change this if your output directory is different
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
